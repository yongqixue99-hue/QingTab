# 轻页 QingTab 0.2.7 Lite 维护版验证记录

验证日期：2026-08-26
状态：后台修复候选；尚未替换当前驻留版本，尚未创建或发布 `v0.2.7` GitHub Release。

## 本版目标

0.2.7 不改变 0.2.6 Lite 的响应优先体验，不恢复遮罩、UI 自动化、注入、地址栏自动化或全局键鼠钩子。本版只修复边界路由、Explorer 恢复、并发安全、IPC、旧日志迁移、退出确认和正式发布流程。

## 已完成修复

- 盘符只接受 ASCII `A–Z`；设备命名空间、命名管道、空服务器 UNC 和伪盘符交还 Windows 原生路径。
- 保留正常盘符、UNC 与 WSL UNC；UNC 去重改为大小写敏感，避免丢弃 Linux/Samba 上仅大小写不同的真实文件夹。
- 补齐 `RPC_E_RETRY`、`RPC_E_SERVERCALL_REJECTED`、`RPC_E_CONNECTION_TERMINATED` 和 `RPC_E_SERVER_DIED_DNE`，分别进入有界重试或 Shell 重连。
- 导航和失败清理前确认“原标签全部保留且只新增一个候选标签”；并发标签变化时不导航、不关闭身份不明的标签。
- 命名管道客户端连接后 500 ms 内不发送完整请求时，只丢弃该连接并立即重建监听，避免通信入口长期被占住。
- 驻留实例首次启动时检查当前错误日志及两个归档；发现旧版异常消息、路径或调用栈时，用固定隐私安全记录替换。
- `--exit` 现在有 5 秒驻留互斥体释放确认；卸载脚本在进程没有完全退出时停止，不再继续删除状态。
- 窗口类名比较读取完整类名，避免长类名被截断成 `CabinetWClass` 后误认。
- 项目版本提升为 `0.2.7 / 0.2.7.0`，旧 `v0.2.6` 标签保持不动，后续只允许新建 `v0.2.7`。
- GitHub 正式流程改用 SignPath 官方 Artifact ID 签名请求，不再接收 PFX；SignPath 返回的 EXE 必须再次通过 Authenticode、证书链和可信时间戳验证后才可打包。
- 便携包和源码包补齐 README 引用的隐私、签名、发布流程与内存基准文档；演示 GIF 改用公开仓库绝对地址，避免包内相对链接失效而显著增大下载包。

## 自动化验证

- Release 构建目标：0 警告、0 错误。
- 行为检查：`PASS: 237 QingTab behavior checks`。
- 生命周期检查：`PASS: 23 QingTab lifecycle checks`。
- 签名与发布流程静态门禁：`PASS: 22 QingTab signing pipeline checks`。
- NuGet 已知漏洞检查：未发现易受攻击的直接或传递包。
- 未签名候选 EXE：`125440` 字节，FileVersion `0.2.7.0`，SHA-256 `F80CE246005B2C5E3D87424F56D12763220EDDFBC8CCDEFD82E7B8E6338EE406`。
- 便携 ZIP 与源码 ZIP 的最终 SHA-256 由发布流水线在包完成后写入同目录校验文件和 Release 报告，不在包内自引用。
- 源码 ZIP 独立重建：0 警告、0 错误，237 + 23 项检查通过；源码包内签名流程检查为 22 项通过。
- 正式程序集仍不含 UIAutomation、WindowsBase 或已撤回的视觉遮罩类型。

## 尚未执行的桌面干扰测试

遵守用户正在使用电脑的约束，本轮没有打开、切换、关闭 Explorer 标签或窗口，也没有重启 Explorer、注销、休眠或替换当前驻留程序。以下项目必须在用户明确允许、保存工作后另行执行：

- 休眠/唤醒后的首次与连续打开；
- Explorer 实际重启后的自动重连；
- 用户同时按 Ctrl+T 与轻页请求的竞争压力测试；
- 回收站、控制面板、库、网络、WSL 和 UNC 的真实点击回归；
- 30 分钟与 24 小时驻留测试。

## 已知边界

- Windows 没有公开稳定的“创建标签并同时指定目录”API，新标签仍可能短暂显示 Explorer 默认页。
- 同步 Shell/COM 调用若被 Explorer 本身卡住，15 秒业务预算不能强制中断正在执行的跨进程调用；彻底隔离需要更重的 STA 架构，不进入本维护版。
- 轻量原生身份检查能拒绝已经观察到的多标签并发，但无法在没有 UIA/注入的前提下数学上证明标签创建者身份。
- SignPath Foundation 申请获批并配置组织、项目、策略和 API Token 之前，0.2.7 仍是未签名开发候选，不能宣传为“已验证发布者”。
